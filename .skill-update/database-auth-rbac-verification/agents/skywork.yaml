interface:
  display_name: "Database Auth RBAC Verification"
  short_description: "Verify DB, auth, services, Pages, and gates"
  default_prompt: "Use $database-auth-rbac-verification to map DOCX-driven multilingual legal services, preserve legal-claim and publication gates, synchronize the browser-only static mirror, verify PostgreSQL persistence, Auth.js/OIDC, RBAC, Pages/CDN evidence, and run complete browser regressions."
